version: '3'

services:
  sennet-db:
    image: "postgres:16"
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    volumes:
      -  /opt/data/postgres:/var/lib/postgresql/data
  sennet-uwsgi:
    image: "sennet/integrated-maps-ui-prod:latest"
    ports:
      - "5000:5000"
    expose:
      - 5000
    volumes:
      - /opt/integrated-maps-ui/secret:/opt/integrated-maps-ui/secret/
      - /opt/data:/opt/data/
      - /opt/pipeline_outputs:/opt/pipeline_outputs/
      - /opt/media:/opt/media/
      - /opt/integrated-maps-ui/uwsgi:/opt/integrated-maps-ui/uwsgi
  sennet-nginx:
    image: "nginx:alpine"
    volumes:
      - /etc/nginx/nginx.conf:/etc/nginx/nginx.conf
      - /etc/letsencrypt:/etc/letsencrypt
      - /etc/nginx/conf.d:/etc/nginx/conf.d
      - /opt/media/media:/opt/media/
      - /opt/integrated-maps-ui/sennet/static/:/opt/integrated-maps-ui/sennet/static/
    ports:
      - "5080:5080"
      - "4433:4433"
    depends_on:
      - sennet-uwsgi
      - sennet-shinycell2
  sennet-shinycell2:
    image: "sennet/integrated-maps-ui-shinycell:latest"
    ports:
      - "3838:3838"
    volumes:
      - /opt/shiny-server:/data
      - /opt/shiny-server/shiny-server:/srv/shiny-server
    restart: unless-stopped 
